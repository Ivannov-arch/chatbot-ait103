# ============================================================
# XMUM Campus Chatbot — Python Dependencies
# Install with: pip install -r requirements.txt
# ============================================================

# Supabase Python client
supabase>=2.4.0

# FastAPI & ASGI server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Data validation
pydantic>=2.7.0
pydantic-settings>=2.2.0

# Environment variable loading
python-dotenv>=1.0.1

# HTTP client (used internally by supabase-py and for tests)
httpx>=0.27.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.6

# Data extraction (dataset pipeline)
pdfplumber>=0.11.0          # Ekstrak teks dari PDF handbook
pytesseract>=0.3.10         # OCR for scanned PDFs
pypdfium2>=5.8.0            # Fast PDF rendering to images
requests>=2.32.0            # HTTP request ke website XMUM
beautifulsoup4>=4.12.0      # Parse HTML halaman website
lxml>=5.2.0                 # HTML parser cepat untuk BeautifulSoup

# Optional: code quality
# black>=24.4.0
# ruff>=0.4.0
