# placeholder_tests_init.py
# tests/__init__.py
#
# Marks the `tests` directory as a Python package.
# Run all tests with:  pytest
# Run with coverage:   pytest --cov=chatbot --cov=api --cov-report=term-missing
